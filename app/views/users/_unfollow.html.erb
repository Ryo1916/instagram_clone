<%= link_to user_relationship_path(user_id: current_user.id, id: followed_id(current_user, user)),
            method: :delete,
            remote: true do %>
  <div class="btn btn-outline-info">
    Unfollow
  </div>
<% end %>
