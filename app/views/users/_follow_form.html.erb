<% unless user == current_user %>
  <div id="follow_form_<%= user.id %>" style="display:inline;">
  <% if current_user.following?(user) %>
    <%= render 'unfollow', user: user, style: 'display:inline;' %>
  <% else %>
    <%= render 'follow', user: user %>
  <% end %>
  </div>
<% end %>
